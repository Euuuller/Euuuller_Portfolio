# Normalize line endings to LF for text files
* text=auto eol=lf

# Treat common binary assets appropriately
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.ico binary

# SVG is text but should not be normalized by some tools
*.svg text eol=lf

# Preserve CRLF for batch files and Windows scripts (if any)
*.bat eol=crlf
*.cmd eol=crlf